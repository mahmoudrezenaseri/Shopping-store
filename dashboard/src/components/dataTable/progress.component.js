import {
    CSpinner
} from '@coreui/react';

const Progress = () => {

    return (
        <div style={{ width: "100%", textAlign: "center" }}>
            <CSpinner size="lg" />
        </div>
    );
};

export default Progress